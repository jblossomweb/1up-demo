{
  "name": "oneup-season",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "array.prototype.find": "^1.0.0"
  },
  "devDependencies": {
    "mocha": "*",
    "should": "*",
    "sinon": "*"
  },
  "scripts": {
    "test": "mocha --recursive --reporter dot",
    "tests": "mocha --recursive --reporter spec"
  }
}
